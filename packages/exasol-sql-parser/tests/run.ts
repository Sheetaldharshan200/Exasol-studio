// Golden-test harness (Phase 5+): each corpus file must parse cleanly; each
// caret scenario must yield the expected suggestion set. Populated per TASKS.md.
console.log("exasol-sql-parser: no generated parser yet — run `npm run generate` (Java >= 11), then add corpus + caret tests.");
