{
  "name": "@exasol-studio/agent-core",
  "version": "0.1.0",
  "private": true,
  "description": "Headless agentic AI core for Exasol Studio \u2014 HTTP+SSE server, provider registry, agent loop.",
  "type": "module",
  "main": "dist/agent-core.cjs",
  "scripts": {
    "build": "node ../../scripts/gen-ui-map.mjs && esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/agent-core.cjs --external:node:*",
    "dev": "node --experimental-strip-types src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.0",
    "@ai-sdk/google": "^2.0.0",
    "@ai-sdk/openai": "^2.0.0",
    "@ai-sdk/openai-compatible": "^1.0.0",
    "@exasol/exasol-driver-ts": "^0.4.1",
    "ai": "^5.0.0",
    "ws": "^8.21.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}